# Job Listener — Frontend (Prototype)

This folder contains a minimal React + Vite frontend scaffold for the Job Listener dashboard.

Run locally:

```bash
npm install
npm run dev
```

Build:

```bash
npm run build
```
